import ItemListContainer from "../../components/ItemListContainer/ItemListContainer";

export default function Cart () {
    return(
        <div>
            <h1 className="title"> Aca vamos a ver el carrito.</h1>
        </div>  
    );
}